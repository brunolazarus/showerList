Place a 16x16 or 22x22 PNG named `trayIconTemplate.png` here.

The `Template` suffix tells macOS to treat it as a template image,
which automatically adapts to light/dark mode in the menu bar.

Export a black icon with transparency — macOS will handle coloring.
