{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "pnpm -w run build-spotify && tsc && electron .",
    "typecheck": "tsc --noEmit",
    "build:check": "tsc --noEmit"
  },
  "dependencies": {
    "@showerlist/spotify-client": "workspace:*",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^34.0.0"
  }
}
