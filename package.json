{
  "name": "showerlist",
  "private": true,
  "scripts": {
    "build-spotify": "pnpm --filter @showerlist/spotify-client run build",
    "typecheck": "pnpm --filter @showerlist/spotify-client run build && pnpm -r typecheck",
    "test": "vitest run",
    "lint": "eslint .",
    "build": "pnpm -C apps/desktop run build:check"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.57.1",
    "vitest": "^1.6.0"
  }
}
